--- a/contrib/rtorrent.fc
+++ b/contrib/rtorrent.fc
@@ -1,4 +1,5 @@
 HOME_DIR/.rtorrent.rc	--	gen_context(system_u:object_r:rtorrent_home_t,s0)
 HOME_DIR/.rtsession(/.*)?	gen_context(system_u:object_r:rtorrent_session_t,s0)
+HOME_DIR/torrents(/.*)? 	gen_context(system_u:object_r:rtorrent_download_t,s0)
 
 /usr/bin/rtorrent	--	gen_context(system_u:object_r:rtorrent_exec_t,s0)
--- a/contrib/rtorrent.if
+++ b/contrib/rtorrent.if
@@ -58,3 +58,48 @@
 
 	rtorrent_role($2, $1)
 ')
+
+#######################################
+## <summary>
+##      Role access for rtorrent
+##      Parts missing in the distribution
+## </summary>
+## <param name="user_role">
+##      <summary>
+##      The role associated with the user domain.
+##      </summary>
+## </param>
+## <param name="user_domain">
+##      <summary>
+##      The user domain.
+##      </summary>
+## </param>
+#
+interface(`rtorrent_role_missing',`
+	gen_require(`
+		type rtorrent_download_t;
+	')
+
+	manage_dirs_pattern($2, rtorrent_download_t, rtorrent_download_t)
+	manage_files_pattern($2, rtorrent_download_t, rtorrent_download_t)
+')
+
+#######################################
+## <summary>
+##      Administer the rtorrent application.
+##      Parts missing in the distribution
+## </summary>
+## <param name="domain">
+##      <summary>
+##      Domain allowed access.
+##      </summary>
+## </param>
+## <param name="role">
+##      <summary>
+##      Role allowed access.
+##      </summary>
+## </param>
+#
+interface(`rtorrent_admin_missing',`
+	rtorrent_role_missing($2, $1)
+')
--- a/contrib/rtorrent.te
+++ b/contrib/rtorrent.te
@@ -1,5 +1,14 @@
 policy_module(rtorrent, 1.0.0)
 
+require {
+  type user_t;
+  role user_r;
+  type staff_t;
+  role staff_r;
+  type sysadm_t;
+  role sysadm_r;
+}
+
 ########################################
 #
 # Declarations
@@ -31,6 +40,12 @@
 type rtorrent_session_t;
 userdom_user_home_content(rtorrent_session_t)
 
+# Downloaded files: manageable by rtorrent, manageable by user
+type rtorrent_download_t;
+userdom_user_home_content(rtorrent_download_t)
+files_mountpoint(rtorrent_download_t)
+
+
 ########################################
 #
 # rtorrent local policy
@@ -45,6 +60,10 @@
 manage_dirs_pattern(rtorrent_t, rtorrent_session_t, rtorrent_session_t)
 manage_files_pattern(rtorrent_t, rtorrent_session_t, rtorrent_session_t)
 
+# Manage the downloaded files
+manage_dirs_pattern(rtorrent_t, rtorrent_download_t, rtorrent_download_t)
+manage_files_pattern(rtorrent_t, rtorrent_download_t, rtorrent_download_t)
+
 corenet_tcp_bind_generic_node(rtorrent_t)
 corenet_tcp_bind_rtorrent_port(rtorrent_t)
 corenet_tcp_connect_all_ports(rtorrent_t)
@@ -82,6 +101,14 @@
 	rsync_exec(rtorrent_t)
 ')
 
-optional_policy(`
-	xdg_manage_downloads_home(rtorrent_t)
-')
+
+########################################
+#
+# We still need to add some small things
+#
+
+rtorrent_admin_missing(sysadm_t, sysadm_r)
+rtorrent_role_missing(user_r, user_t)
+
+rtorrent_role(staff_r, staff_t)
+rtorrent_role_missing(staff_r, staff_t)
